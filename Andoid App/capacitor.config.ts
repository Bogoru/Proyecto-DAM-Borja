import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.app.base',
  appName: 'Proyecto DAM Borja',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
